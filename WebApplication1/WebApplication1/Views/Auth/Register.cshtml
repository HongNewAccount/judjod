@{
    ViewData["Title"] = "Create User";
    Layout = "_Layout";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card border-0 shadow-sm">
                <div class="card-header py-3 text-white" style="background:var(--color-accent);">
                    <h6 class="mb-0 fw-bold"><i class="fas fa-user-plus me-2"></i>Register</h6>
                </div>
                <div class="card-body p-4">
                    @if (ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show small py-2" role="alert">
                            @ViewBag.ErrorMessage
                            <button type="button" class="btn-close btn-sm" data-bs-dismiss="alert"></button>
                        </div>
                    }
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success small py-2">@TempData["SuccessMessage"]</div>
                    }

                    <form method="post" asp-action="Register">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Username <span class="text-danger">*</span></label>
                            <input type="text" name="username" class="form-control" placeholder="Enter username" required autocomplete="username" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Password <span class="text-danger">*</span></label>
                            <input type="password" name="password" class="form-control" placeholder="Enter password" required autocomplete="new-password" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Confirm Password <span class="text-danger">*</span></label>
                            <input type="password" name="confirmPassword" class="form-control" placeholder="Re-enter password" required autocomplete="new-password" />
                        </div>

                        @if (Context.Session.GetString("IsSuperAdmin") == "true")
                        {
                            <div class="mb-4 p-3 bg-light rounded">
                                <label class="form-label fw-semibold mb-2 d-block">Initial Settings</label>
                                <div class="d-flex flex-column gap-2">
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="form-check form-switch mb-0">
                                            <input class="form-check-input" type="checkbox" name="chatEnabled" id="regChat" value="true" checked />
                                        </div>
                                        <label for="regChat" class="small fw-semibold mb-0">
                                            <i class="fas fa-comments me-1" style="color:var(--color-accent);"></i>Chat Access
                                        </label>
                                    </div>
                                    <div class="d-flex align-items-center gap-3">
                                        <div class="form-check form-switch mb-0">
                                            <input class="form-check-input" type="checkbox" name="projectAccess" id="regProject" value="true" checked />
                                        </div>
                                        <label for="regProject" class="small fw-semibold mb-0">
                                            <i class="fas fa-columns me-1 text-primary"></i>Project Access
                                        </label>
                                    </div>
                                </div>
                            </div>
                        }

                        <div class="d-flex gap-2">
                            <a href="@Url.Action("Index", "User")" class="btn btn-secondary btn-sm flex-fill">Cancel</a>
                            <button type="submit" class="btn btn-modal-action btn-sm flex-fill">
                                <i class="fas fa-user-check me-1"></i>Create User
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
